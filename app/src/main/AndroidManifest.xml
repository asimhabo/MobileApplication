<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.merveerdem.yourguide">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity" />
        <activity
            android:name=".ViewStops"
            android:theme="@style/AppTheme.NoActionBar" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AboutActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ContactUsActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FavouritesActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MyNextBusActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PlacesOfInterestActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AccommodationPlacesOfInterest"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SignUp"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ViewMyNextChosenBus"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ViewBuses"
            android:theme="@style/AppTheme.NoActionBar" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ViewOnMapActivity"
            android:parentActivityName=".AccommodationPlacesOfInterest"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NearestHospital"
            android:label="@string/title_activity_nearest_hospital" />
        <activity
            android:name=".JourneyPlannerActivity"
            android:label="@string/title_activity_journey_planner" />
        <activity
            android:name=".NearestRestaurants"
            android:label="@string/title_activity_nearest_restaurants" />
        <activity
            android:name=".NearestAccommodation"
            android:label="@string/title_activity_nearest_accommodation" />
        <activity
            android:name=".Journeytohere"
            android:label="@string/title_activity_journeytohere" />
        <activity
            android:name=".AtmPlacesOfInterest"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AvmPlacesOfInterest"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CinemaPlacesOfInterest"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".GymPlacesOfInterest"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HospitalPlacesOfInterest"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LanguageSchoolPlacesOfInterest"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RestaurantPlacesOfInterest"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>